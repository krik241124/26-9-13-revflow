# 登录 CL，切换到与 config/runtime.json 相同的国家。
# F12 -> Network -> 打开一个 SKU 详情 -> 找到 GetDetail 请求。
# 右键 Copy as cURL，将完整内容保存到同目录 getdetail.curl.txt。
# 此文件只提供说明，不是可执行 cURL，也不能直接改名后运行。
# 切国家或登录失效后重新复制，不将真实 cURL 放进源码交付包。
