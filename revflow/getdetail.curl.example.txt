# Legacy/manual fallback only. Normal Web UI runs capture the shared CL login session automatically.
# RevFlow reads cl_country_guid / cl_language from config/markets.json for the active market.
# If manual diagnosis is ever needed: capture a complete GetDetail cURL and save it as getdetail.curl.txt.
# Never put a real cURL, cookie, or token into the source release package.
